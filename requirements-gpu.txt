# GPU stack — requires CUDA 12.4+, install after requirements.txt
# pip install -r requirements.txt -r requirements-gpu.txt

# PyTorch (CUDA 12.4)
torch>=2.5.0
torchvision>=0.20.0

# SGLang — local LLM serving with RadixAttention
sglang[all]>=0.4.0

# FlashInfer — CUDA attention kernels
flashinfer-python>=0.2.0

# Triton — GPU kernels
triton>=3.1.0

# Quantization
bitsandbytes>=0.45.0
autoawq>=0.2.7

# HuggingFace model loading
transformers>=4.47.0
accelerate>=1.2.0
huggingface-hub>=0.27.0
